{
  "name": "@barrelny/shopify",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@7.1.0"
}
