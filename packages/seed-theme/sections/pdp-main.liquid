<div>
  <div>
    <h1>{{ product.title }}</h1>
    <p>{{ product.price | money_without_trailing_zeros }}</p>
  </div>
  {% render 'product-gallery' %}
  {% render 'product-form' %}
  {% render 'product-details' %}
</div>
